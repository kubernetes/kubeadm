# AUTOGENERATED by https://git.k8s.io/kubeadm/kinder/ci/tools/update-workflows
version: 1
summary: |
  This workflow implements a sequence of tasks for testing kubeadm join
  and upgrade when the "kube-proxy" and "coredns" ConfigMaps are missing.
  test grid > https://testgrid.k8s.io/sig-cluster-lifecycle-kubeadm#kubeadm-kinder-no-addonslatest
  config    > https://git.k8s.io/test-infra/config/jobs/kubernetes/sig-cluster-lifecycle/kubeadm-kinder-addons.yaml
vars:
  initVersion: "{{ resolve `ci/latest` }}"
tasks:
- import: upgrade-no-addon-config-maps-tasks.yaml
